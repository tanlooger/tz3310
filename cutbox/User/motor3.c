#include "motor3.h"




u8 set_motor3_run(u8 target_position)
{
    /*
    int i=0;
    if(get_motor3_position()==target_position){
        //延时12毫秒
        for(i=0; i<100000; i++); //一个循环12个机器周期, 一个指令周期10ns
        if(get_motor3_position()==target_position)return 1;
    }
    if(get_motor3_position() > 4){
        return 2;
    }
    */
    
    
    
    
    
}

